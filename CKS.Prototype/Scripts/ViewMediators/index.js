var cks = cks || {};
cks.viewMediators = cks.viewMediators || {};
cks.viewMediators.index = function (indexVm) {
    var _$content = $("div#content");
    //***Remove GeoLocation
    //ko.applyBindings(indexVm, _$content.find("div#index-view")[0]);
};